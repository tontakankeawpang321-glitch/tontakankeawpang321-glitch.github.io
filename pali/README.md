# Thai Law AI (Browser-only)

1) ใส่ API key ใน index.html บรรทัด window.GOOGLE_API_KEY
2) รัน `npx serve .` แล้วเปิด http://localhost:3000 หรือ 8080
3) อัปขึ้นโฮสต์ static ได้ทันที (GitHub Pages/Firebase Hosting)
